# /home/fugbert/www/web-stockfish-gemini/frontend/Dockerfile

# Stage 1: Alpine Builder - MUST be corrected to use a reliable tag
# Use 'latest-alpine-builder' instead of a specific, retired version tag
FROM dunglas/frankenphp:latest-alpine-builder AS caddy_builder 

# ... (xcaddy build commands) ...

# Stage 2: Final Alpine Runtime Image - MUST be corrected
FROM dunglas/frankenphp:latest-alpine

# ... (copy commands) ...
